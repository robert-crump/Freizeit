# Freizeit v2 progress log

## 2026-07-30 — Issue #35 (partial): rename Explore -> Map; fix search substring false-positive

- Full /grill-me session held first (every branch of the redesign resolved with the user,
  decisions posted to #35 as a comment before any code was touched). Mid-implementation the
  user asked to stop after finishing only what was already started and file the rest as
  follow-up issues, so this session lands just the rename + a standalone search bug fix; the
  actual full-screen search overlay/oval redesign is split out to #37, which carries the full
  grill-me decision spec forward.
- Renamed `Explore` -> `Map` across code, not just the nav label (explicit grill-me decision):
  `ui/explore` package -> `ui/map` (`git mv`, all 7 main files + `FilterAndSortTest.kt`),
  `ExploreScreen`/`ExploreViewModel`/`ExploreUiState`/`ExploreMapHolder` -> `MapScreen`/
  `MapViewModel`/`MapUiState`/`MapViewHolder`, `FreizeitDestination.EXPLORE` -> `.MAP` (route
  `"explore"` -> `"map"`), every `explore_*` string resource -> `map_*` (wording unchanged),
  `tab_explore` ("Explore") -> `tab_map` ("Map"), plus comments in `PoiMap.kt`/`PlaceMiniMap.kt`/
  `MarkerIcons.kt`/`HomeViewModel.kt`/`CheckInScreen.kt`/`CheckInViewModel.kt`/`VerdictDao.kt`
  that referenced the old names.
- Bug fix found during the grill-me session itself: `filterAndSort`'s search matched via a plain
  `name.contains(query, ignoreCase = true)`, so a query like "Len" would wrongly match a POI
  named "Bottleneck" (the substring "len" sits mid-word, bott-LEN-eck). New `matchesSearch()` in
  `MapViewModel.kt` anchors the match at word boundaries instead — `wordStartIndices()` finds
  every letter/digit run's start, then checks whether the query is a case-insensitive prefix of
  the name starting from any of those points. This still matches "Len" or "Caf" against "Leni's
  Café" (first/second word) and "hidden gem" against "Our Hidden Gem" (anchored at "Hidden"),
  but no longer matches "len" against "Bottleneck". `filterAndSort`'s public signature is
  unchanged (still takes `searchQuery: String?`), so no caller needed updating.
- Deliberately did NOT change `ExploreViewModel`'s (now `MapViewModel`'s) live per-keystroke
  `setSearchQuery`/debounced-search/suggestion-dropdown behavior in this session — that whole
  interaction model is being replaced by the full-screen search overlay from #35's grill-me
  decisions (committed-vs-live search state, `focusTarget` hoisting to share with the overlay,
  removing the old dropdown), which is explicitly out of scope for this session per the user's
  "finish what you started, file the rest as follow-ups" instruction. An earlier draft of
  `MapViewModel.kt` in this session had already added that committed-search/`focusTarget`
  plumbing ahead of the overlay that would consume it; reverted back to the original
  live-search/debounce shape (renamed only) so this commit stays a clean, self-contained rename
  + bug fix with no half-built API surface. #37 has the full remaining implementation checklist.
- `FilterAndSortTest.kt`: renamed the old substring test to describe word-boundary-prefix
  matching (query "HAR"->"CHAR" against "Charlie"), and added two new cases: a "Bottleneck"
  POI confirming "len" no longer matches it, and a "Leni's Café" POI confirming both "Len" and
  "Caf" match it (first and second word). `FreizeitDestinationTest.kt`'s route list updated
  `"explore"` -> `"map"`.
- Tests: 146 total (was 144), 0 failures — the +2 nets out `FilterAndSortTest`'s 13 -> 15 cases
  (rename + 2 new); confirmed via its own XML report (15/15 pass) and `FreizeitDestinationTest`'s
  (3/3 pass). `compileDebugKotlin`/`compileDebugUnitTestKotlin`/`assembleDebug`: all clean, no
  new warnings (the pre-existing `FlowPreview` opt-in warning on the `searchQuery.debounce(...)`
  call is unchanged, now at `MapViewModel.kt`'s new location).
- Not verified on device: the rename touches every string resource and file path involved in the
  Map tab, so it's worth a quick sanity check that the tab still renders "Map" and the map/
  search/layers panel all still work identically to before — purely mechanical rename risk, not
  new Compose wiring, but not yet eyeballed on Robert's phone.
- Issue #35 left open (not closed): most of its scope (the actual visual/interaction redesign)
  is deferred to #37.

## 2026-07-29 — Explore layers: replace "All POIs" row with a Velometrics-style chip row

- No GitHub issue; user asked directly, worked out via a full /grill-me session before any code
  was touched (chip selection model, Favorites/Want to go placement, filter exclusivity, chip
  ordering, chip visuals/color scope, selected-state indication, chip row layout, chip-vs-search
  interaction — every branch resolved with the user first, several answers landing differently
  than the Velometrics source material they were modeled on).
- Biggest decision, arrived at only after checking Velometrics' actual chip code: single-select,
  not the multi-select #33 gave categories. Tapping a chip shows only that category; tapping the
  active chip again clears it. This collapses `ExploreViewModel`'s `activeCategories: Set<String>`
  + `showAllPois: Boolean` into one nullable `activeCategory: String?` (mirrors Velometrics'
  `activePoiChip` pattern), still mutually exclusive with `favoritesOnly`/`wantToGoOnly`/search.
  `selectCategory(category)` replaces `toggleCategory`/`selectAllPois` entirely.
- `filterAndSort`'s `activeCategories: Set<String>` + `showAll: Boolean` params collapsed to a
  single `activeCategory: String?`; the `showAll` branch (`it.category in activeCategories`)
  became `activeCategory != null -> it.category == activeCategory`. `FilterAndSortTest.kt`
  rewritten for the single-select shape (16 → 13 cases; dropped the multi-select-specific cases,
  kept priority-order coverage for search/favorites/category).
- Favorites and Want to go stay in the Layers FAB panel (now just those two `LayerRow`s) per
  explicit choice — categories move out entirely rather than folding Favorites/Want to go into
  the chip row too. `LayersPanel`'s `categories`/`activeCategories`/`showAllPois` params and the
  "All POIs" `LayerRow` are gone; `CategoryLayerRow` (the old dimmed/bold multi-select row from
  #33) is deleted outright, not deprecated.
- New `PoiCategoryChipRow` in `ExploreScreen.kt`: categories sorted alphabetically by
  `categoryDisplayName` (not `CATEGORY_ORDER`'s curated hue-spacing order, which stays untouched
  for `MapStyle.kt`'s order-agnostic `categoryIconExpression()`). Explicit decision: chips carry
  no per-category color at all — reusing the map markers' own move away from category color
  (`ab93390`/`2d31b3e`), every chip shares the single theme-driven `markerForegroundColor`/
  `markerBackgroundColor` pair instead. Unselected: transparent container, outline + icon + label
  in the foreground color. Selected: container fills with the foreground color, icon + label flip
  to the background color — deliberately mirroring the marker circle/icon contrast relationship,
  not a new color scheme. New `categoryIcon(category)` exported from `MarkerIcons.kt` (previously
  private `CATEGORY_ICONS`) so the chip row's leading icon is the exact same icon as that
  category's marker. Icon/label sizing left at `FilterChip`'s own defaults, matching how
  Velometrics' own chip code has no explicit size overrides either.
- Chip row floats over the map's top edge (inside the map `Box`, top-aligned) rather than sitting
  in the non-overlapping search-bar `Row` above it — explicit choice to match Velometrics' visual
  style over Freizeit's own layout convention. Hidden entirely whenever the search-suggestions
  dropdown is showing (`searchSuggestions.isEmpty()` gate) rather than stacking the two, since
  both float over the same map-top area.
- `explore_all_pois_filter` string resource deleted (no longer referenced anywhere).
- Tests: 144 total (was 147), 0 failures — the -3 nets out `FilterAndSortTest`'s 16 → 13 cases;
  confirmed via each test's own XML report, no other test file touched or regressed.
  `compileDebugKotlin`/`compileDebugUnitTestKotlin`/`assembleDebug`: all clean (the pre-existing
  `FlowPreview` opt-in warning on `ExploreViewModel.kt`'s `searchQuery.debounce(...)` is unchanged
  from before this session).
- Not verified on device: the chip row's floated-over-map placement, the fill/outline selected-
  state contrast in both light and dark theme, and the hide-while-search-suggestions-show
  behavior are all new Compose wiring — needs a real run on Robert's phone.

## 2026-07-29 — POI marker redesign: category icons on themed circles

- No GitHub issue; user asked directly for this after a full /grill-me session covering every
  branch of the design (color-vs-icon, layer badging, basemap theming, icon set, marker size,
  bitmap generation strategy, cluster styling, stroke contrast, UI scope, fallback icon, position
  dot) before any code was touched.
- Biggest decision: category color is dropped from the map entirely — `categoryColor()` (11
  hand-tuned hues) stays in `ui/common/CategoryUi.kt` for other UI (untouched, out of scope per
  explicit decision) but no longer drives the Explore map's POI markers. A Material icon is now
  the sole category signal; the circle itself is purely theme-driven (dark circle in dark theme,
  light circle in light theme).
- New `ui/explore/MarkerIcons.kt`: `CATEGORY_ICONS` maps all 11 categories + `UNKNOWN_CATEGORY`
  fallback (`Icons.Filled.Place`) to a `material-icons-extended` `ImageVector` (already a
  dependency, no new library). `rememberMarkerBitmaps(darkTheme)` rasterizes each into a real
  `android.graphics.Bitmap` at runtime via `rememberVectorPainter` + `CanvasDrawScope` (circle
  fill, opposite-color stroke, centered tinted icon) — chosen over checking in ~22 static PNG/
  vector assets so colors/sizing stay driven by code constants, not hand-maintained files.
  Marker diameter deliberately kept at the old CircleLayer's 24px (12px radius) rather than
  growing it for icon legibility — explicit tradeoff accepted during the interview; some icons
  (Museum, Interests, Handyman) may read as a blob at that size and could need swapping once seen
  on a real device.
- `ui/explore/MapStyle.kt`: added `POSITRON_STYLE_URL` (CARTO's light counterpart to the existing
  `dark-matter-gl-style`) and `mapStyleUrl(darkTheme)`; `markerBackgroundColor`/
  `markerForegroundColor` (fixed hexes, independent of the app's dynamic Material theme, same
  pattern as the existing `POSITION_DOT_COLOR`); `categoryIconExpression()` (new `icon-image`
  match expression, mirrors the existing `categoryColorExpression()` still used by
  `PlaceMiniMap.kt`'s Home-carousel mini map — deliberately not touched, out of scope, it's a
  different map with no "layers" toggle).
- `PoiMap.kt`: `poiCircleLayer()` replaced by `poiSymbolLayer()` (`iconImage` instead of
  `circleColor`). `isSystemInDarkTheme()` read once per composable body and captured by the
  `DisposableEffect(mapView)` closure that builds the style — since `ExploreMapHolder` already
  retains the `MapView`/style across Home<->Explore tab switches for the life of the Activity,
  this reads the theme even less often than the interview's "only re-theme on next screen entry"
  answer, not more; a live system-theme flip mid-session is a no-op until the Activity recreates,
  matching the decision not to rebuild sources/layers on the fly. Favorites/Want to go/All POIs
  layers deliberately get no marker distinction beyond filtering, matching today's behavior
  exactly (explicit decision, not an oversight). Cluster circles (`clusterCircleLayer`,
  `clusterCountLayer`) switched from `MaterialTheme.colorScheme.secondary` to the same themed
  background/foreground pair as individual markers, and their stroke/text color now also flips
  opposite the fill — extending the individual-marker contrast fix to clusters for consistency,
  not separately asked about in the interview but a direct consequence of the same problem
  (white-on-white text on a light basemap) already solved there. "You are here" position dot
  (`POSITION_DOT_COLOR`) explicitly left untouched per the interview, even though it's no longer
  guaranteed to contrast against the now-optional light basemap.
- Tests: 147 total, 0 failures (`testDebugUnitTest`) — notably the ~28 Robolectric
  `UnsatisfiedLinkError` failures documented as pre-existing since #10/#13/#15/#23 and reconfirmed
  through #33 are gone in this environment; not investigated further since nothing in this change
  touches DAO/Robolectric setup, and it reads as an environment fix rather than a regression risk.
  `compileDebugKotlin` and `assembleDebug` both clean.
- Not verified on device: this is the first real exercise of a light MapLibre style in the app
  (previously always dark-matter) and of runtime `style.addImage()`/`SymbolLayer` icons (previously
  only `CircleLayer`s existed) — needs a real run in both system light and dark mode to confirm
  icon legibility at the kept-small marker size, the Positron basemap's look, and that cluster
  circles read correctly against it.
- Follow-up, same day: bumped `MARKER_DIAMETER` 24dp → 30dp (25% larger, icon scales with it via
  the existing `ICON_SCALE` fraction) per direct user feedback that icons were too small. Swapped
  `playground`'s icon from `Icons.Filled.Toys` to `Icons.Filled.ChildCare` — checked the actual
  `material-icons-extended` AAR's `classes.jar` first (no literal "Playground" icon exists in that
  library; only `ChildCare`/`ChildFriendly`/`Toys` matched "child"-ish names), confirming
  `ChildCare` compiles before committing rather than guessing at a class name.
- Follow-up: extended the same icon-in-circle treatment to `PlaceMiniMap.kt`'s `SuggestionsMiniMap`
  (the embedded map inside Home's swipe-deck `SuggestionCard`), which still had the old
  `CircleLayer` + `categoryColorExpression()`. Now shares `mapStyleUrl`/`categoryIconExpression`/
  `rememberMarkerBitmaps` with the Explore map. `categoryColorExpression()` had no remaining
  callers afterward, so it and its now-unused imports were deleted from `MapStyle.kt`. Explicit
  scope decision (asked, not assumed): the small `CategoryDot` swatch next to the category label
  stays a plain color dot — it's shared with `PlaceDetailSheet.kt`, which this redesign still
  doesn't touch.
- Bug fix, same follow-up: the Suggestion Card's markers rendered 1.5x larger than Explore's.
  `poiSymbolLayer()`'s `iconSize` carried over the old dot-radius logic's selected/unselected
  scale (14/9 ratio), but `SuggestionsMiniMap`'s only caller always passes a single POI with
  `selectedPoiId == that POI's own id` — every marker there was therefore always "selected" and
  always scaled up. Removed the size-by-selection expression entirely so both maps render icons
  at the same 1x size; `POI_ICON_SELECTED_SCALE` deleted as now-unused.
- No device verification on any of these three follow-ups beyond `compileDebugKotlin`/
  `testDebugUnitTest`/`assembleDebug` staying clean after each — the user is iterating from
  on-device screenshots between requests, not this agent.

## 2026-07-26 — Issue #33: Explore layer menu: reorder, multi-select categories, declutter

- Only `ready-for-agent` issue open. Split out of #31's grill-me session specifically so it
  could be reviewed/reverted independently of the "Want to go" verdict feature.
- Reorder was already done: #31's log entry notes the top rows were inserted in
  Favorites/Want to go/All POIs order already, "without needing #33's reorder work" — verified
  by reading `ExploreScreen.kt`'s `LayersPanel` before touching anything, no code change needed
  for that part.
- The real work was converting category filtering from single-select to multi-select and
  gating its visibility on "All POIs": `ExploreViewModel`'s `selectedCategory: String?` (a
  fourth mutually-exclusive top-level layer, same tier as Favorites/Want to go/All POIs) is
  gone, replaced by `activeCategories: Set<String>` that's only ever consulted while
  `showAllPois` is true — categories are now a filter *within* All POIs, not a peer layer.
  `selectCategory(category)` (single-select toggle) replaced by `toggleCategory(category)`
  (independent set membership toggle, zero-selected allowed).
- `selectAllPois()` resets `activeCategories` to every known category each time it's turned on
  (from off to on) — read literally from the issue's "Entering All POIs activates every category
  by default," not preserving whatever subset was active from a previous All-POIs session. Reads
  the category list synchronously off `uiState.value.categories` (the same derived list already
  shown in the panel) rather than adding a second `poiDao.observeAll()` collector just to get a
  snapshot.
- `filterAndSort()`'s `selectedCategory: String?` param replaced by `activeCategories: Set<String>`;
  the `showAll` branch changed from unconditionally `true` to `it.category in activeCategories`,
  and the previous `else -> it.category == selectedCategory` fallback branch (the single-category
  layer) is gone entirely — `else -> false`, since category filtering has no path outside
  `showAll` anymore. `FilterAndSortTest.kt` rewritten accordingly (13 → 16 cases): added explicit
  coverage for multi-category matching, zero-active-categories showing nothing, and
  showAll=false ignoring a non-empty active set.
- `ExploreScreen.kt`: `LayersPanel`'s category rows are now wrapped in `if (showAllPois)` so they
  disappear only when switching to Favorites/Want to go, matching the issue's "stays visible while
  switching between categories, hides only on Favorites/Want to go." New `CategoryLayerRow`
  composable replaces the shared `LayerRow` (which carries a `Switch`) for category rows
  specifically — active/inactive now reads via `FontWeight.SemiBold` + full-opacity text vs.
  `FontWeight.Normal` + 0.4-alpha text, per the issue's explicit "closer to background when
  inactive, still legible" instruction. Vertical padding dropped from `LayerRow`'s 2.dp to 0.dp
  for the "tighter vertical spacing... denser list" ask.
- Deliberately left as-is: the top three rows (Favorites/Want to go/All POIs) keep `LayerRow`
  and its `Switch` unchanged — the issue's decluttering ask was scoped to category rows only.
- Tests: 147 total (was 144), 32 failed (unchanged) — the +3 are net new `FilterAndSortTest`
  cases (13 → 16), all green (confirmed via its own XML report: 16/16 pass, 0 failures). The 32
  pre-existing failures are the same 28 Robolectric `UnsatisfiedLinkError` classes
  (PendingVisitDaoTest/PoiCustomNameDaoTest/VerdictDaoTest/VisitDaoTest/BackupRepositoryTest/
  PoiImportTest) plus the 4 `HomeSwipeDeckMathTest` failures, both documented as pre-existing
  since #31/#32 — confirmed unchanged by class/count, no new failing class introduced.
  `compileDebugKotlin`/`compileDebugUnitTestKotlin`: clean.
- Not verified on device: the multi-select category rows (tap-to-toggle, zero-selected showing
  an empty map, the text-contrast active/inactive treatment, All POIs resetting the set on
  re-entry) are all new Compose wiring — needs a real run on Robert's phone.

## 2026-07-26 — Issue #31: "Want to go" places

- Design worked out via a full /grill-me pass before writing any code (verdict
  mutual-exclusivity, icon choice, `PlaceDetailSheet` interaction model, merged-vs-separate
  Home deck, ranking parity, Explore filter chip, geofencing scope, naming — all resolved
  with the user first; the Explore layer-menu redesign that came up mid-session was split
  out to #33 rather than bundled in here).
- New `Verdict.VALUE_WANT_TO_GO` constant alongside `VALUE_FAVORITE`. No schema/PK
  migration needed: `Verdict` was already one row per `placeId`, so the two verdicts are
  mutually exclusive for free — setting one overwrites the other via the existing
  `setVerdict()` upsert.
- Icon is `Icons.Filled.Bookmark` / `Outlined.BookmarkBorder` (new `WantToGoBlue` tint in
  `ui/theme/Color.kt`), not a star — the user's own reasoning during the grill-me pass: a
  star reads too much like a second "favorite," where a bookmark reads as its own distinct
  "saved for later" concept.
- `PoiDao` gained `observeByVerdictValues(values)` alongside the existing
  `observeFavorites()` — Home's deck now queries both verdict values together; the
  favorites-only geofence sync path (`FreizeitApplication`/`AppContainer`) is untouched,
  still calling `observeFavorites()` directly, per the explicit decision that want-to-go
  places get no geofencing/DWELL notifications.
- `SuggestionEngine`: the hard filter now accepts either verdict value; `Suggestion` gained
  a `verdictValue` field so the UI can tell which bucket a card came from. Deliberately no
  scoring changes beyond that — both verdict types compete on equal footing, no type-based
  weighting. The visit-frequency term needed no special-casing: it's already keyed off
  actual visit history, which is naturally empty for a never-visited want-to-go place.
- `HomeViewModel`/`HomeScreen.kt`: renamed `hasFavorites`/`hasFavoritesWithinRadius` to
  `hasVerdictedPlaces`/`hasVerdictedPlacesWithinRadius` (now genuinely about both buckets,
  not just favorites) and `onUnfavorite` to `onRemoveVerdict` throughout — a merged single
  deck means these no longer mean "favorite" specifically. The card's action button now
  shows heart or bookmark depending on `suggestion.verdictValue`, no separate badge needed.
- `PlaceDetailSheet.kt`: new `BookmarkButton`, inserted between the existing Edit and
  Favorite buttons (row order: Edit, Bookmark, Favorite, per explicit placement
  instruction). Two independent icon buttons, not a segmented control — tapping one while
  the other is set silently clears it, mirroring the existing single-heart interaction
  model.
- `ExploreViewModel`/`ExploreScreen.kt`: added a "Want to go" layer row, inserted directly
  after "Favorites" (so the final top-row order already reads Favorites/Want to go/All
  POIs without needing #33's reorder work). `filterAndSort`'s `favoriteIds` parameter
  renamed to `verdictIds` since it's now shared by whichever single verdict-based filter is
  active — updated `FilterAndSortTest.kt`'s named-argument call sites to match.
- Explicitly not touched, per the grill-me decisions: no geofencing/DWELL notifications for
  want-to-go places; the Explore layer-menu multi-select/reorder/decluttering work (tracked
  separately in #33).
- Tests: 144 total (was 140), 32 failed (unchanged) — new `SuggestionWantToGoTest` (4
  cases: a want-to-go place appears in the deck, a stray verdict value that's neither
  favorite nor want-to-go is still excluded, each suggestion carries its own verdict value,
  a favorite and a want-to-go place score identically under identical conditions), all
  green. The 32 pre-existing failures are the same 28 Robolectric `UnsatisfiedLinkError`
  classes plus the 4 `HomeSwipeDeckMathTest` failures already documented as pre-existing in
  #32's entry above — confirmed unchanged by class/count.
  `compileDebugKotlin`/`compileDebugUnitTestKotlin`: clean.
- Not verified on device: the merged deck's card icon swap, the new Explore "Want to go"
  filter chip, and the `PlaceDetailSheet`'s Edit/Bookmark/Favorite row are all new Compose
  wiring with no existing on-device confirmation to lean on — needs a real run on Robert's
  phone.

## 2026-07-26 — Issue #32: POI search: Focus jumps back to first position while typing

- Design worked out via a full /grill-me pass before writing any code (fix scope, debounce
  parity with Explore, shared-vs-duplicated constant, test coverage, clear-button debounce
  tradeoff, rememberSaveable vs remember, device-verification ownership — all resolved with
  the user first, same approach as #21).
- Root cause: `CheckInScreen.kt`'s `OutlinedTextField` bound `value` directly to
  `state.searchQuery`, which round-trips through `CheckInViewModel`'s un-debounced 5-way
  `combine` (including `poiDao.observeAll()`) on `Dispatchers.Default` before echoing back —
  Compose can't reconcile the cursor position against a stale/delayed value, so it resets to
  the start. Identical bug already fixed in `ExploreScreen`'s search (`10ac5d3`) via a local
  `rememberSaveable` buffer decoupled from ViewModel state; Check-in's search bar (added later
  in #30) never got the same treatment.
- Fix mirrors Explore's pattern exactly rather than diverging: `CheckInScreen.kt` gained a
  local `searchText` (`rememberSaveable`) driving the text field, with `onValueChange`/
  `onClear` updating both the local buffer and the ViewModel. `CheckInViewModel.kt` gained its
  own private `SEARCH_DEBOUNCE_MS = 250L` (deliberately duplicated, not shared with
  `ExploreViewModel`'s constant of the same name/value, so the two screens stay independently
  tunable) and `.debounce(SEARCH_DEBOUNCE_MS)` on the `searchQuery` flow feeding the `combine`.
- Deliberately not special-cased: `clearSearch()` still goes through the same debounced flow,
  so Check-in's clear button carries the same ~250ms lag before reverting to the
  favorites-nearby list that Explore's clear button already has — an accepted existing
  tradeoff from the pattern being mirrored, not something to fix differently per screen.
- No new test: cursor/focus behavior isn't practically unit-testable without a Compose UI test
  harness (this codebase has none), and Explore's identical fix shipped with no test either —
  same precedent followed rather than introducing new test infrastructure as a side effect of
  a one-issue bug fix.
- Tests: 140 total, 32 failed — identical count and failures to unmodified `main` (confirmed
  via `git stash` comparison), so zero regressions from this change. The 32 are the 28
  pre-existing Robolectric `UnsatisfiedLinkError` failures (documented since #10/#13/#15/#23)
  plus 4 `HomeSwipeDeckMathTest` failures that are also pre-existing as of `df63710` (confirmed
  failing identically on unmodified `main` — a swipe-deck alpha-math regression from the
  "closed/rainy favorites" change that landed just before this session, unrelated to #32 and
  not investigated further here). `compileDebugKotlin`: clean (the new `debounce()` call emits
  the same pre-existing, unaddressed `FlowPreview` opt-in warning `ExploreViewModel.kt:135`
  already has).
- Not verified on device this session — user will verify directly on their phone.

## 2026-07-26 — Issue #22: Investigate hardcoded Cologne fallback location in HomeViewModel

- Picked up on explicit user override: the issue carries no `ready-for-agent` label (no labels
  at all — filed as a self-assigned follow-up from #21, never triaged). Proceeded anyway since
  the user named it directly and it turned out to be fully specified once investigated.
- Answered the issue's three questions by reading code, not guessing:
  - **Interaction with #21's radius filter**: none. `HomeViewModel.uiState`'s `combine` block
    (`HomeViewModel.kt:110-138`) feeds `SuggestionEngine.withinRadius` the raw `loc` from the
    `location` `StateFlow` — real GPS fix or `null` — never the `FALLBACK_LAT`/`FALLBACK_LON`
    constants. `withinRadius` already treats `null` as "skip filtering" (its doc comment cites
    #21 directly). The fallback constants are read in exactly two places, both camera/fetch
    centering, never suggestion filtering: `HomeViewModel.refreshLocation()`'s
    `weatherRepository.refresh(...)` call and `PoiMap.kt`'s initial camera target.
  - **Why "Cologne" and whether it should exist at all**: it shouldn't have been Cologne —
    that was a bug, not a documented decision. `git log -S` traces `FALLBACK_LAT = 50.94` /
    `FALLBACK_LON = 6.96` (Köln's actual coordinates) to the 2026-07-06 v2-rewrite squash
    commit, which predates the POI extraction bbox decision entirely. `tools/poi_extraction`
    fixed the bbox to Aachen center +/-20km on 2026-07-16 (`4de3c36`, lat 50.5956-50.9550, lon
    5.7996-6.3682) and nobody circled back to the fallback constants. Cologne's longitude
    (6.96) sits outside the bbox's max lon (6.3682) — the old fallback pointed to a spot with
    zero POI coverage, so a no-GPS user would see an empty Explore map and get weather for a
    city ~60km from every POI in the app. The fallback itself should exist (better than 0,0 or
    a blank map/weather widget when GPS is unavailable) — only the coordinates were wrong.
  - Fixed both call sites (`HomeViewModel.kt`, `PoiMap.kt`) to the actual bbox center
    (50.7753, 6.0839 — Aachen), with comments naming the real source (`tools/poi_extraction`)
    instead of the incorrect "Cologne" label, and a note in `HomeViewModel.kt` cross-referencing
    #22 so the history of the mistake stays visible.
- No test references either fallback constant directly (checked `app/src/test` for
  `HomeViewModel`/`PoiMap`); the 50.94/6.96 literals in `SuggestionFixture.kt` and
  `OpenMeteoClientTest.kt` are unrelated fixture coordinates, not references to the constants.
- Tests: 138 total, 28 failed (unchanged) — same pre-existing Robolectric `UnsatisfiedLinkError`
  set (MapLibre native init) documented since #10/#13/#15/#23 and reconfirmed as of #29.

## 2026-07-25 — Issue #29: Closest-100 geofence selection for large favorite counts

- Picked up on explicit user override: #29 is the only `ready-for-agent` issue, but its
  "Blocked by #28" note is a formal-tracker relationship, not a technical one — #28's code
  (`GeofenceSyncManager` registering every favorite, no cap) is already implemented and on
  `main` (confirmed by reading the file directly); #28's `ready-for-human` label is about its
  Play Store background-location policy surface, not about #29's prerequisite being unbuilt.
  Three prior sessions (#25/#26/#27's log entries) deferred #29 for this reason; the user
  explicitly asked to implement it regardless this time, confirmed live: "I already received a
  notification for a favorite nearby when the app was open" — i.e. #28's registration path is
  demonstrably working already.
- Split `GeofenceSyncManager`'s single `sync()` into two: `sync(enabled, favorites)` (unchanged
  trigger — favorites/toggle change, app-open) and a new `rerank(enabled, favorites, location)`.
  Under 100 favorites, `sync()` still registers everyone immediately, unchanged from #28. Over
  100, `sync()` doesn't rank anything itself — it replays whichever closest-100 selection the
  last `rerank()` persisted to a new `GeofenceStateRepository.selectedFavoriteIds` (same
  DataStore-backed `Set<String>` pattern as the existing `insidePlaceIds`). `rerank()` is the
  only thing that ever recomputes that set, and it's wired to fire from exactly one place: a new
  `GeofenceLocationMonitor` (`FusedLocationProviderClient.requestLocationUpdates` with
  `setMinUpdateDistanceMeters(500f)`, no periodic interval logic beyond Play Services' own
  batching) — armed/disarmed in `FreizeitApplication.onCreate` alongside the same
  `autoCheckInEnabled` collector that already drives `sync()`, so it's never triggered by app-open
  or a timer, per the issue's explicit ask.
- Ranking itself is a new pure function, `selectClosestFavorites(favorites, lat, lon, limit)` in
  `util/GeofenceFavoriteRanking.kt` (Haversine via the existing `GeoDistance.metersBetween`,
  matching `rankNearbyForCheckIn`'s existing pure-function-for-testability convention) — under
  `limit` returns the list unchanged, over it sorts by distance and takes `limit`.
- Lazy un-favorite deregistration (explicit AC: "not required to be immediate") falls out of how
  `sync()`'s replay path resolves ids: it looks each persisted selected id up via
  `PoiDao.getById()` directly, not by filtering the current `favorites` param against it — so an
  id that's no longer a favorite still resolves (the `Poi` row survives un-favoriting, only the
  `Verdict` row changes) and keeps its geofence registered until the next `rerank()` overwrites
  the whole selected-ids set from scratch and drops it.
- First-time-crossing-100 bootstrap (no `rerank()` has ever run, no persisted selection to
  replay): `sync()` falls back to `favorites.take(100)` in arbitrary DB order rather than doing
  any location-based ranking itself — deliberately dumb, to keep `sync()` truly rank-free per the
  issue's "not on app-open" requirement; a real closest-100 selection supersedes it as soon as
  the location monitor's first significant-change fix arrives.
- Tests: new `GeofenceFavoriteRankingTest` (5 cases — under limit unchanged, exactly-at-limit
  unchanged, over limit keeps exactly the closest N out of a shuffled 150-favorite fake set
  matching the issue's own "testable with >100 entries" acceptance criterion, over-limit results
  sorted closest-first, empty input). No test added for `GeofenceSyncManager`/
  `GeofenceLocationMonitor` themselves — both need a real Android `Context`/Play Services client,
  same reason the pre-#29 `GeofenceSyncManager` had no test file either.
  `compileDebugKotlin`: clean. `testDebugUnitTest`: 138 total (was 133), 28 failed (unchanged) —
  the +5 are the new `GeofenceFavoriteRankingTest` cases, all green (confirmed via its own XML
  report: 5/5 pass); the 28 failures are the same 6 pre-existing Robolectric
  `UnsatisfiedLinkError` classes documented since #10/#13/#15/#23
  (PendingVisitDaoTest/PoiCustomNameDaoTest/VerdictDaoTest/VisitDaoTest/BackupRepositoryTest/
  PoiImportTest), confirmed by summing each class's failure count and by class name — no new
  failing class introduced.
- Not verified on device: everything in this issue is new code with no existing on-device
  confirmation to lean on (unlike #28's sync path, which the user already saw fire once) —
  the significant-location-change trigger actually firing Play Services' `LocationCallback`,
  the >100 favorites cap taking effect, and the lazy-deregistration behavior all need a real run,
  ideally with a synthetic >100-favorite dataset since reaching that count organically is
  unlikely during normal testing.

## 2026-07-25 — Issue #25: Amend product doc for the geofencing reversal

- Only unblocked `ready-for-agent` issue: #29 is blocked by #28, which is
  still `ready-for-human` (not agent-pickable). #25 itself has no blockers.
- `docs/ideas/freizeit-2.0-family-concierge.md`'s "Not Doing" entry for
  "Geofencing / background location" struck through (not deleted) with a
  dated addendum explaining the reversal: #20 (check-in) needs background
  proximity detection to prompt a check-in while the app is closed, which a
  next-open prompt can't provide; #28 is the issue that actually implements
  the geofencing, flagged `ready-for-human` for its Play Store policy
  surface rather than a routine agent pickup. Kept the original rationale
  text intact above the strikethrough so the doc still reads as a decision
  history, per the issue's explicit ask.
- Docs-only change, no code touched — no test run, no `progress.txt` test
  count line needed.

## 2026-07-25 — Issue #28: Auto-detect proximity notification for favorites

- Flagged `ready-for-human` (not `ready-for-agent`) in the issue itself —
  background location + a Play Store disclosure surface. Implemented with
  Robert pairing directly rather than picked up autonomously, which the
  next-issue skill's checklist treats as satisfying the same human-review
  intent the label exists for.
- No new `Favorite` entity, no new "last checked in" storage: favorites are
  already `PoiDao.observeFavorites()` (poi ⋈ verdict='favorite', #17), and
  the shared cross-source cooldown ("manual or notification, one clock per
  favorite") falls straight out of the existing `visit` table — added
  `VisitDao.lastVisitedAt(placeId)` (`MAX(visitedAt) WHERE placeId = ...`)
  instead of inventing a parallel timestamp store. `Visit.SOURCE_NOTIFICATION`
  added alongside the existing `SOURCE_MANUAL`.
- Geofence radius reuses `CheckInViewModel.CHECKIN_FAVORITE_RADIUS_METERS`
  (200m, #23) by direct import rather than redefining it, per the issue's
  own "same radius as the manual check-in list's 200m cutoff."
- New `com.example.freizeit.data.geofence` package: `GeofenceSyncManager`
  (registers/removes Play Services geofences — one per favorite, ENTER+EXIT
  transitions, `removeGeofences` then `addGeofences` on every resync since
  Play Services has no "update" call), `GeofenceBroadcastReceiver` (handles
  both the Play Services transition intent and the notification's own
  "Check in"/"Dismiss" action intents — one receiver so all three share the
  same "recompute the one active notification" path), `GeofenceNotifications`
  (channel setup + build/show/cancel).
- "Only the closest favorite's notification is ever shown" is solved
  structurally rather than by tracking per-favorite notification IDs: every
  notification reuses one fixed ID, so there is never more than one to begin
  with. What still needs tracking is *which* favorites the phone is
  currently physically inside (Play Services only tells you about the
  transition that just fired, not the standing set) — new
  `GeofenceStateRepository` persists that as a `Set<String>` of placeIds via
  DataStore, deliberately kept separate from `SettingsRepository` since it's
  runtime state, not a user-facing preference. On every ENTER/EXIT the
  receiver updates that set, then re-derives the nearest non-cooling-down
  member from the transition's `triggeringLocation` (pure, unit-tested
  `closestEligibleFavorite`/`isCoolingDown` in new
  `util/GeofenceNotificationSelection.kt`) and shows or cancels accordingly.
  A cooling-down closest favorite does NOT suppress the notification
  entirely — the next-nearest non-cooling-down one still gets it, since nothing
  in the issue text says a *whole spot* is picked before applying cooldown,
  only that "that favorite" is suppressed.
- Dismiss vs. cooldown, deliberately kept independent: `ACTION_DISMISS` only
  cancels the notification, touching neither the inside-set nor the visit
  log, so a fresh ENTER later can re-show it — matches "a dismissed or
  ignored notification does NOT suppress" without needing a "was dismissed"
  flag anywhere.
- Sync trigger: `FreizeitApplication.onCreate` launches a
  `combine(settingsRepository.autoCheckInEnabled, poiDao.observeFavorites())
  .distinctUntilChanged()` collector calling `GeofenceSyncManager.sync(...)`
  — covers the toggle flipping on (after #24's permission chain completes),
  favorites being added/removed, and process start (geofences don't survive
  process death the way a DataStore flag does, so re-registering on every
  cold start is required, not just an optimization).
- Added `com.google.android.gms:play-services-location:21.3.0` (this
  project's first Play Services dependency — MapLibre, not Google Maps, was
  already in use, so `google()` in `settings.gradle` existed but nothing
  had pulled from it yet) and `kotlinx-coroutines-play-services:1.7.3` for
  `Task<T>.await()` rather than hand-rolling a `suspendCancellableCoroutine`
  wrapper.
- Known gaps, deliberately left out as beyond the issue's acceptance
  criteria rather than silently swallowed: geofences do not survive device
  reboot (no `BOOT_COMPLETED` receiver added — the app-open resync covers
  it in practice for a family day-trip app opened regularly, but a
  reboot-then-never-reopened device would miss auto check-ins until next
  open); un-favoriting a place while standing inside its still-active
  notification does not proactively cancel that notification (no EXIT
  event fires for a geofence that's merely removed from registration, only
  for an actual physical exit).
- Tests: new `GeofenceNotificationSelectionTest` (7 cases covering
  `isCoolingDown`'s same/previous-day boundary and `closestEligibleFavorite`'s
  nearest-pick / cooldown-skip / all-cooling-down / empty-input cases) —
  pure JVM tests, no Robolectric, following the `CheckInRankingTest`/
  `isReadyForBanner` convention. Two new cases added to the existing
  `VisitDaoTest` for `lastVisitedAt`. `compileDebugKotlin`: clean.
  `testDebugUnitTest`: 133 total (was 124), 28 failed (was 26) — the +2 are
  the two new `VisitDaoTest` cases failing via the same pre-existing
  Robolectric `UnsatisfiedLinkError` (MapLibre native init) that already
  fails every other test in that class; confirmed via the XML report that
  no new failing test class was introduced.
- On-device pass with Robert caught a real bug the unit tests couldn't:
  `geofencePendingIntent` in `GeofenceSyncManager` was built with
  `FLAG_IMMUTABLE`, which crashed geofence registration at runtime with
  `ApiException: 10: PendingIntent must be mutable` — Play Services writes
  into that intent before redelivering it, so it can't be immutable (unlike
  the notification's own action `PendingIntent`s in `GeofenceNotifications`,
  which are correctly immutable since nothing external mutates those).
  Switched to `FLAG_MUTABLE`; confirmed the app now launches and registers
  geofences without crashing.
- Still not verified on device: actual ENTER/EXIT transitions firing (needs
  physically visiting/leaving a favorite, or a mock-location run), the
  multi-favorite "closest wins" behavior when two favorites are near each
  other, notification action buttons actually logging a `Visit` without
  opening the app, and the permission-revoked-mid-flight `SecurityException`
  path. Given the HITL flag on this issue, worth exercising these before
  merge rather than trusting the Play Services API surface read correctly.


