<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions für Standort und Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Freizeit"
        tools:targetApi="31">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.ActivityListActivity"
            android:label="Aktivitäten"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.activity.ActivityEditActivity"
            android:label="Aktivität bearbeiten"
            android:parentActivityName=".ui.activity.ActivityListActivity" />

        <activity
            android:name=".ui.activity.RandomActivityActivity"
            android:label="Zufällige Aktivität"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:label="Einstellungen"
            android:parentActivityName=".ui.MainActivity" />

        <activity
            android:name=".ui.settings.FavoriteLocationEditActivity"
            android:label="Favorit-Adresse"
            android:parentActivityName=".ui.settings.SettingsActivity" />

    </application>

</manifest>